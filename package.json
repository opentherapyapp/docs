{
  "name": "open-therapy-docs",
  "private": true,
  "type": "module",
  "description": "Documentation site for Open Therapy, published to docs.opentherapy.app",
  "scripts": {
    "dev": "mint dev",
    "check": "mint validate && mint broken-links && npm run check:images",
    "check:images": "node --experimental-strip-types scripts/check-images.ts",
    "screenshots": "playwright test --config scripts/screenshots.config.ts && npm run size-images",
    "size-images": "node --experimental-strip-types scripts/size-images.ts",
    "wordmark": "python3 scripts/wordmark.py"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0",
    "sharp": "^0.35.3"
  }
}
